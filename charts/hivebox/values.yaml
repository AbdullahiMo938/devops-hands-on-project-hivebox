replicaCount: 2

image:
  repository: ghcr.io/abdullahimo938/hivebox
  pullPolicy: Always
  tag: "v2"

service:
  type: ClusterIP
  port: 3000

probes:
  readinessPath: /readyz #  readiness probe setting!